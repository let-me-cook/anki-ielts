[project]
name = "anki-ielts"
version = "0.1.0"
description = "Utilities for fetching shared Anki decks into raw/"
requires-python = ">=3.9"
dependencies = [
  "PyYAML>=6.0,<7",
]

[tool.uv]
package = false
